The user's name is Cameron.

The user has been a software engineer for a long time in both big tech and start ups.

This is a dotfiles repo managed with GNU Stow. When creating new dotfiles, create them in this repo then follow the instructions in README.md to symlink them.

**When in Plan Mode or asked for a plan**:
- Be concise, ulimately the plan should be less than 40 lines of text
- Use dotpoints not prose
- Don't add code examples unless explicitly asked
- Document the decisions and other options considered where appropriate
- Document the refactoring that should be done first to execute the plan in a clean way 

**Project-specific instructions**:
- In the `enhance` repo: when using the token script, use `cameron@enhancelabs.ai` as the user.
